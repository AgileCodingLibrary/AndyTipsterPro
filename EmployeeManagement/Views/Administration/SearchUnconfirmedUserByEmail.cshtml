
@{
    ViewBag.Title = "Delete unconfirmed users";
}

    <div class="col-xs-12">
        <p class="lead lead-lg text-center">Delete Unconfirmed User</p>
        <p>This feature is used to remove users that have not confirmed their emails, not interested anymore in our products.</p>
        <p>This feature can also be used to remove a user who have lost their confirmation email. Removing a user is saftey feature to avoid hackers.</p>
        <p>Feature will only search and remove user that has<strong> un confirmed account ONLY.</strong></p>
    </div>


<div class="container">
    <div class="row">
        <form asp-controller="Administration" asp-action="DeleteUnConfirmedUser"
              method="post" class="form-horizontal" role="form">

            @Html.AntiForgeryToken()

            <div class="col-md-8">
                <h4>Please enter user email.</h4>
                <p>If user is not found, customer has never registered on our website.</p>              

                <div class="input-group">
                    <span class="input-group-btn">
                        <button class="btn btn-primary" type="submit">Search</button>
                    </span>
                    <input type="text" class="form-control" name="email" id="email">
                </div>
            </div>
        </form>
    </div>
</div>
