@model PaginatedList<ApplicationUser>

@{
    ViewBag.Title = "Customer Dashboard";
}

<div class="heads">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h2>Customers</h2>
                <p>Dashboard offers one stop for viewing all your customers and their subscriptions.</p>
            </div>
        </div>
    </div>
</div>

@if (Model != null && Model.Count() > 0)
{
    <div class="page-content">
        <div class="container">
            <div class="row confirm">
                <div class="col-sm-10 col-md-12">
                    <table class="table table-striped table-responsive" style="font-size:small;">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Email</th>
                                <th>User Name</th>
                                <th>SendEmails</th>
                                <th>Elite Allowed</th>
                                <th>Combo Allowed</th>
                                <th>UK-Racing Allowed</th>
                                <th></th>

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var customer in Model)
                            {
                                <tr>
                                    <td>@customer.FirstName  @customer.LastName </td>
                                    <td><a asp-action="UserDetails" asp-controller="Administration" asp-route-userId="@customer.Id">@customer.Email</a></td>
                                    <td>@customer.UserName</td>
                                    <td>@customer.SendEmails</td>
                                    <td>@customer.CanSeeElitePackage</td>
                                    <td>@customer.CanSeeComboPackage</td>
                                    <td>@customer.CanSeeUKRacingPackage</td>

                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
}
else
{
    <h2>There are no Customers.</h2>
}

@{
    var prevDisabled = !Model.PreviousPage ? "disabled" : "";
    var nextDisabled = !Model.NextPage ? "disabled" : "";
}

<a asp-action="UserDashboard"
   asp-route-pageNumber="@(Model.PageIndex - 1)"
   class="btn btn-default @prevDisabled">
    Previous
</a>

<a asp-action="UserDashboard"
   asp-route-pageNumber="@(Model.PageIndex + 1)"
   class="btn btn-default @nextDisabled">
    Next
</a>

@*<section>
    <ul class="pagination">
        <li class="@prevDisabled"><a href="#">«</a></li>
        <li class="active"><a href="#">1 <span class="sr-only">(current)</span></a></li>
        <li><a href="#">2</a></li>
        <li><a href="#">3</a></li>
        <li><a href="#">4</a></li>
        <li><a href="#">5</a></li>
        <li><a href="#">6</a></li>
        <li><a href="#">7</a></li>
        <li><a href="#">8</a></li>
        <li><a href="#">9</a></li>
        <li><a href="#">10</a></li>
        <li class="@nextDisabled"><a href="#">»</a></li>
    </ul>
</section>*@